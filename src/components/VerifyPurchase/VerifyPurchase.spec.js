import { render } from "@testing-library/react"
import VerifyPurchase from "."

describe('VerifyPurchase component', () => {
    it('Should  render', () => {
        render(<VerifyPurchase />)
    })
})
